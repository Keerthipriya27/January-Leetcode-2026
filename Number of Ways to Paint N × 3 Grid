class Solution:
    def numOfWays(self, n: int) -> int:
        MOD = 10**9 + 7
        same = 6
        diff = 6
        for _ in range(1, n):
            same, diff = (same * 3 + diff * 2) % MOD, (same * 2 + diff * 2) % MOD
        return (same + diff) % MOD
